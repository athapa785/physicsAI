# React-era API runtime. The retired Streamlit prototype had a much larger
# LangChain/audio dependency tree; the maintained app needs only these packages.
fastapi>=0.115.0,<1.0.0
openai>=2.0.0,<3.0.0
uvicorn[standard]>=0.30.0,<1.0.0
